# Publish Configuration for flutter-shadcn-ui

<!-- Auto-generated by publish-manager skill on 2026-06-23. Edit if the workflow changes. -->

## Package Manager

- **Type**: `pub.dev` (private registry at `https://pub.zuzu.dev`)
- **Packages**: `shadcn_ui` (main), `shadcn` (CLI)
- **Public packages**: None
- **Private packages**: Both packages are internal

## Scripts

- **Pre-publish**: None — create a release branch manually
- **Publish**: `make publish` (runs `dart pub publish`) — use `dart pub publish --force` to skip interactive confirmation
- **Post-publish**: None configured
- **Restore dev**: None configured

## Workflow

1. Bump version in `pubspec.yaml`
2. Update `CHANGELOG.md` files
3. `git checkout -b publish-<version>`
4. Commit and push the branch
5. `dart pub publish --force` (the `-f` flag auto-answers the confirmation prompt)
6. `git tag v<version> && git push origin v<version>`
7. Merge publish branch to main and delete it

## Notes

- The `-f` flag on `dart pub publish` is required because the tool runs non-interactively
- The CLI package (`shadcn`) at version `0.0.0` has not been published independently
